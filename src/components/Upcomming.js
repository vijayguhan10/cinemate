// Upcomming.js
import React from "react";
import MovieUI from "../components/MovieUI";

const Upcomming = ({ url }) => {
  return (
    <div>
      <MovieUI url={url} />
    </div>
  );
};

export default Upcomming;
