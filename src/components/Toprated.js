// Toprated.js
import React from "react";
import MovieUI from "../components/MovieUI";

const Toprated = ({ url }) => {
  return (
    <div>
      <MovieUI url={url} />
    </div>
  );
};

export default Toprated;
